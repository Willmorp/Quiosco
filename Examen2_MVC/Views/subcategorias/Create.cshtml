@model Examen2_MVC.Models.subcategoria


<div class="modal fade" id="modal_crear_subcategoria">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
                <h4>Crear Categorias</h4>
            </div>
            @using (Ajax.BeginForm("Create", "subcategorias", new AjaxOptions()
            {
                HttpMethod = "POST",
                UpdateTargetId = "listar_subcategoria",
                OnSuccess = "mensajeCrear('#modal_crear_subcategoria')"
            }))
            {
                @Html.AntiForgeryToken()
                <div class="modal-body">

                    @Html.ValidationSummary(true, "", new { @class = "text-danger" })



                    <div class="row">

                        <div class="col-lg-6">

                            <div class="form-group">
                                <span>Crear Categotia</span>
                                @Html.EditorFor(model => model.nombresubcategoria, new { htmlAttributes = new { @class = "form-control" } })
                                @Html.ValidationMessageFor(model => model.nombresubcategoria, "", new { @class = "text-danger" })
                            </div>
                        </div>

                        <div class="col-lg-6">

                            <div class="form-group">
                                <span>Crear Categotia</span>
                                @Html.DropDownList("idcategoria", null, htmlAttributes: new { @class = "form-control" })
                                @Html.ValidationMessageFor(model => model.idcategoria, "", new { @class = "text-danger" })
                            </div>
                        </div>
                    </div>



                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-default pull-left" data-dismiss="modal">Cerrar</button>
                    <button type="submit" class="btn btn-primary">Guardar</button>
                </div>

            }
        </div>
    </div>

</div>