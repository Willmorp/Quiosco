@model IEnumerable<Examen2_MVC.Models.unidaddemedida>

@{
    Layout = null;
}

<div class="row">
    <div class="col-lg-12">
        <div class="box">
            <div class="box-header">
                <h4>Unidad De Medida</h4>
            </div>
            <div class="box-body">
                @Ajax.ActionLink("Crear Medida", "Create", new AjaxOptions {
                HttpMethod="GET",
                InsertionMode = InsertionMode.Replace,
                UpdateTargetId = "contenido_modal",
                OnSuccess = "abrirModal('#modal_crear_medida')",
                })
                <div id="listar_medida">
                    @{Html.RenderPartial("List", Model);}
                </div>
            </div>

        </div>
    </div>
</div>
<div id="contenido_modal"></div>


@*<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nombreunidaddemedida)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nombreunidaddemedida)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.idunidaddemedida }) |
                @Html.ActionLink("Details", "Details", new { id = item.idunidaddemedida }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.idunidaddemedida })
            </td>
        </tr>
    }

</table>*@
